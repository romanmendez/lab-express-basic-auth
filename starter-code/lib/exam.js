const mongoose = require("mongoose");
const examDb = mongoose.Collection("exams");

console.log(examDb.fi);
const level = "A1";

const question = () => {
  const n = console.log(n);
  // mongoose.Collection.aggregate({ $sample: { size: 1 } });
};
